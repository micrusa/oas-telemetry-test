"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const index_1 = require("./index");
const child_process_1 = require("child_process");
vitest_1.vi.mock('child_process');
const mockSpawn = child_process_1.spawn;
function setupMockSpawn(success, stdout = '', stderr = '') {
    const events = {};
    const mockChild = {
        stdout: {
            on: vitest_1.vi.fn((event, cb) => {
                if (event === 'data' && stdout)
                    cb(Buffer.from(stdout));
            })
        },
        stderr: {
            on: vitest_1.vi.fn((event, cb) => {
                if (event === 'data' && stderr)
                    cb(Buffer.from(stderr));
            })
        },
        on: vitest_1.vi.fn((event, cb) => {
            events[event] = events[event] || [];
            events[event].push(cb);
            // Simulate 'close' after a tick
            if (event === 'close') {
                setTimeout(() => cb(success ? 0 : 1), 0);
            }
            if (event === 'error' && !success) {
                setTimeout(() => cb(new Error(stderr || 'error')), 0);
            }
        })
    };
    mockSpawn.mockReturnValue(mockChild);
}
(0, vitest_1.describe)('installDependencies', () => {
    (0, vitest_1.beforeEach)(() => {
        vitest_1.vi.clearAllMocks();
    });
    (0, vitest_1.it)('installs a dependency successfully', async () => {
        setupMockSpawn(true, 'installed', '');
        const opts = {
            dependencies: [{ name: 'lodash' }]
        };
        const result = await (0, index_1.installDependencies)(opts);
        (0, vitest_1.expect)(result.success).toBe(true);
        (0, vitest_1.expect)(result.details[0].success).toBe(true);
        (0, vitest_1.expect)(result.details[0].message).toContain('installed');
        (0, vitest_1.expect)(result.logs).toContain('success: true');
    });
    (0, vitest_1.it)('rejects invalid dependency names', async () => {
        const opts = {
            dependencies: [{ name: 'lodash; rm -rf /' }]
        };
        const result = await (0, index_1.installDependencies)(opts);
        (0, vitest_1.expect)(result.success).toBe(false);
        (0, vitest_1.expect)(result.details[0].success).toBe(false);
        (0, vitest_1.expect)(result.details[0].message).toContain('Invalid dependency name');
        (0, vitest_1.expect)(result.logs).toContain('Invalid dependency name');
    });
    (0, vitest_1.it)('rejects unsafe options', async () => {
        setupMockSpawn(true, 'installed', '');
        const opts = {
            globalOptions: '--save-dev --foo --bar',
            dependencies: [{ name: 'lodash', options: '--silent --badflag' }]
        };
        const result = await (0, index_1.installDependencies)(opts);
        (0, vitest_1.expect)(result.success).toBe(true);
        (0, vitest_1.expect)(result.details[0].success).toBe(true);
        // Only allowed flags are passed
        (0, vitest_1.expect)(result.logs).toContain('Rejected flags');
    });
    (0, vitest_1.it)('uses override options only', async () => {
        setupMockSpawn(true, 'installed', '');
        const opts = {
            globalOptions: '--save-dev',
            dependencies: [{ name: 'lodash', options: '--silent', override: true }]
        };
        await (0, index_1.installDependencies)(opts);
        // Should only use '--silent', not '--save-dev'
        (0, vitest_1.expect)(mockSpawn).toHaveBeenCalledWith('npm', vitest_1.expect.arrayContaining(['install', 'lodash', '--silent']), vitest_1.expect.any(Object));
        (0, vitest_1.expect)(mockSpawn).not.toHaveBeenCalledWith('npm', vitest_1.expect.arrayContaining(['--save-dev']), vitest_1.expect.any(Object));
    });
    (0, vitest_1.it)('logs with verbose=false does not console.log', async () => {
        setupMockSpawn(true, 'installed', '');
        const spy = vitest_1.vi.spyOn(console, 'log').mockImplementation(() => { });
        const opts = {
            dependencies: [{ name: 'lodash' }],
            verbose: false
        };
        await (0, index_1.installDependencies)(opts);
        (0, vitest_1.expect)(spy).not.toHaveBeenCalled();
        spy.mockRestore();
    });
    (0, vitest_1.it)('logs with verbose=true does console.log', async () => {
        setupMockSpawn(true, 'installed', '');
        const spy = vitest_1.vi.spyOn(console, 'log').mockImplementation(() => { });
        const opts = {
            dependencies: [{ name: 'lodash' }],
            verbose: true
        };
        await (0, index_1.installDependencies)(opts);
        (0, vitest_1.expect)(spy).toHaveBeenCalled();
        spy.mockRestore();
    });
    (0, vitest_1.it)('handles install failure', async () => {
        setupMockSpawn(false, '', 'error occurred');
        const opts = {
            dependencies: [{ name: 'lodash' }]
        };
        const result = await (0, index_1.installDependencies)(opts);
        (0, vitest_1.expect)(result.success).toBe(false);
        (0, vitest_1.expect)(result.details[0].success).toBe(false);
        (0, vitest_1.expect)(result.details[0].message).toContain('Exit code');
        (0, vitest_1.expect)(result.logs).toContain('success: false');
    });
});
//# sourceMappingURL=index.test.js.map